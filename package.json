{
  "name": "route53-dns-update",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch './src/**/*.ts' --exec 'ts-node' ./src/index.ts",
    "build": "tsc",
    "docker": "docker build -t fraserbenjamin/route53-dns-update:latest .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fraser Benjamin",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-route-53": "^3.354.0"
  }
}
